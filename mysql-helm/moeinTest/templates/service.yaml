apiVersion: v1
kind: Service
metadata:
  name: {{ include "moeinTest.fullname" . }}
  labels:
    {{- include "moeinTest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moeinTest.selectorLabels" . | nindent 4 }}
